# AKS Monitor Agent — Go

Starter template for AKS cluster monitoring in Go.

## Quick Start

```bash
go run .
```

## Notes

The primary implementation is in [Node.js](../nodejs/). This Go template can delegate to the Node.js agent or run standalone diagnostics.
