# AKS Monitor Agent — .NET

Starter template for AKS cluster monitoring in .NET (C#).

## Quick Start

```bash
dotnet run
```

## Notes

The primary implementation is in [Node.js](../nodejs/). This .NET template can delegate to the Node.js agent or run standalone diagnostics.
